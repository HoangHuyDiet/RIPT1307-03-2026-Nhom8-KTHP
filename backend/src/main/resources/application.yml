spring:
  application:
    name: smart-finance-hub
  datasource:
    url: jdbc:mysql://localhost:3306/smart_finance_hub?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: 145hoang
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

server:
  port: 8080
  servlet:
    context-path: /backend

jwt:
  secret: ${JWT_SECRET:DayLaKhoaBaoMatMacDinhChiDungChoMoiTruongLocalDeTest123456789}
  expiration: 86400000
